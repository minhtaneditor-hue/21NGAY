{
  "name": "21-ngay-video-advisor",
  "version": "1.0.0",
  "description": "Landing page and API for Video Advisor Challenge",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "git add . && git commit -m \"Auto-deploy\" && git push && vercel --prod"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "crypto": "^1.0.1",
    "dotenv": "^16.6.1",
    "express": "^4.22.1",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
